import { useEffect, useState } from "react";
import { Activity, MailCheck, RefreshCw, ShieldCheck } from "lucide-react";

type Data={email:{providerConfigured:boolean;providerName:string|null;testers:number;activeTesters:number;preferencesConfigured:number;welcomeOptIn:number;betaUpdatesOptIn:number;releaseNotesOptIn:number;fullyUnsubscribed:number};reliability:{days:number;metrics:Array<{metricKey:string;count:number}>};privacyNote:string};
async function request<T>(url:string){const response=await fetch(url,{credentials:"include"});const body=await response.json().catch(()=>({})) as any;if(!response.ok)throw new Error(body?.error?.message||"Beta readiness data could not be loaded.");return body as T}
const label:Record<string,string>={client_error:"Client errors",api_error:"API errors",silvi_error:"Silvi errors",release_error:"Release errors",slow_view:"Slow views",failed_refresh:"Failed refreshes"};
export function AdminBetaReadinessPanel(){const[data,setData]=useState<Data|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");async function load(){setBusy(true);setError("");try{setData(await request<Data>("/api/v1/admin/beta-readiness"))}catch(e){setError(e instanceof Error?e.message:"Beta readiness data could not be loaded.")}finally{setBusy(false)}}useEffect(()=>{void load()},[]);return <section className="admin-launch-shell"><header className="admin-launch-heading"><div><small>BETA OPERATIONS</small><h2>Communication & reliability</h2><p>Make sure families can opt out cleanly and that the beta is stable enough to invite more households.</p></div><button className="admin-secondary" type="button" onClick={()=>void load()} disabled={busy}><RefreshCw className={busy?"spin":""}/>{busy?"Refreshing…":"Refresh"}</button></header>{error&&<div className="admin-launch-error">{error}</div>}{data&&<div className="admin-launch-grid"><article className="admin-launch-card"><header><MailCheck/><div><small>TRANSACTIONAL EMAIL</small><h3>{data.email.providerConfigured?`${data.email.providerName} detected`:"Provider still needs connecting"}</h3></div></header><p>{data.email.providerConfigured?"A transactional-email secret is available to the Worker. Welcome and beta-update sending can be enabled once templates are approved.":"Kit Hub will not pretend to send beta email. Add a supported provider secret before marking Email communication as ready."}</p><div className="admin-email-templates"><span><strong>{data.email.welcomeOptIn}</strong><small>Welcome opt-ins</small></span><span><strong>{data.email.betaUpdatesOptIn}</strong><small>Beta-update opt-ins</small></span><span><strong>{data.email.releaseNotesOptIn}</strong><small>Release-note opt-ins</small></span><span><strong>{data.email.fullyUnsubscribed}</strong><small>Fully opted out</small></span></div><p className="admin-launch-note">{data.email.preferencesConfigured} tester{data.email.preferencesConfigured===1?" has":"s have"} saved explicit communication preferences.</p></article><article className="admin-launch-card"><header><Activity/><div><small>RELIABILITY</small><h3>Last {data.reliability.days} days</h3></div></header>{data.reliability.metrics.length?<div className="admin-feature-bars">{data.reliability.metrics.map(metric=><div key={metric.metricKey}><span><strong>{label[metric.metricKey]||metric.metricKey}</strong><small>{metric.count} recorded</small></span></div>)}</div>:<p>No reliability failures have been recorded in this window.</p>}<p className="admin-launch-privacy"><ShieldCheck/>{data.privacyNote}</p></article></div>}</section>}
