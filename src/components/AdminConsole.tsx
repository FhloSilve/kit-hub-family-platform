import { useEffect, useRef, useState } from "react";
import { ArrowLeft, Check, CheckCircle2, CloudCog, ExternalLink, GitBranch, LoaderCircle, Palette, RefreshCw, Rocket, ShieldCheck, Square, TriangleAlert, X } from "lucide-react";
import type { AdminReleaseRun, AdminReleaseStatusResponse } from "../../shared/contracts";
import { ApiError, api } from "../lib/api";
import { AdminFeedbackBoard } from "./AdminFeedbackBoard";
import "../admin.css";
type AdminTheme="orchid"|"apricot"|"periwinkle"|"ocean";type StageState="idle"|"active"|"done"|"failed";const themes:Array<{key:AdminTheme;label:string;hex:string}>=[{key:"orchid",label:"Orchid",hex:"#F2CFF1"},{key:"apricot",label:"Apricot",hex:"#EAB099"},{key:"periwinkle",label:"Periwinkle",hex:"#AFAFDA"},{key:"ocean",label:"Ocean",hex:"#19485F"}];function isSuccessful(run:AdminReleaseRun|null){return Boolean(run?.status==="completed"&&run.conclusion==="success")}function isFinished(run:AdminReleaseRun|null){return Boolean(run?.status==="completed")}
export function AdminConsole({onBack}:{onBack:()=>void}){const[status,setStatus]=useState<AdminReleaseStatusResponse|null>(null),[loading,setLoading]=useState(true),[triggering,setTriggering]=useState(false),[stopping,setStopping]=useState(false),[message,setMessage]=useState<string|null>(null),[error,setError]=useState<string|null>(null),[theme,setTheme]=useState<AdminTheme>(()=>(localStorage.getItem("kit-hub-admin-theme") as AdminTheme)||"ocean"),[completion,setCompletion]=useState<"success"|"failure"|"cancelled"|null>(null),[waitingForRun,setWaitingForRun]=useState(false);const releaseStart=useRef<{runId:number|null;requestedAt:number}|null>(null);async function refresh(){setError(null);try{setStatus(await api.adminReleaseStatus())}catch(e){setError(e instanceof ApiError?e.message:"The release status could not be loaded.")}finally{setLoading(false)}}useEffect(()=>{void refresh()},[]);useEffect(()=>{localStorage.setItem("kit-hub-admin-theme",theme)},[theme]);const latestRun=status?.latestRun??null,start=releaseStart.current;let trackedRun:AdminReleaseRun|null=null;if(latestRun&&start){const created=latestRun.createdAt?new Date(latestRun.createdAt).getTime():0,isOld=start.runId!==null&&latestRun.id===start.runId&&waitingForRun;if(!isOld&&(latestRun.id!==start.runId||created>=start.requestedAt-15000))trackedRun=latestRun}useEffect(()=>{if(!releaseStart.current)return;const timer=window.setInterval(()=>void refresh(),3000);return()=>window.clearInterval(timer)},[triggering,waitingForRun,trackedRun?.id,trackedRun?.status]);useEffect(()=>{if(!trackedRun)return;setWaitingForRun(false);if(!isFinished(trackedRun))return;setCompletion(trackedRun.conclusion==="cancelled"?"cancelled":isSuccessful(trackedRun)?"success":"failure");releaseStart.current=null;setStopping(false)},[trackedRun?.id,trackedRun?.status,trackedRun?.conclusion]);async function triggerRelease(){setTriggering(true);setMessage(null);setError(null);setCompletion(null);releaseStart.current={runId:status?.latestRun?.id??null,requestedAt:Date.now()};setWaitingForRun(true);try{const result=await api.triggerAdminRelease();setMessage(result.message);window.setTimeout(()=>void refresh(),1200)}catch(e){releaseStart.current=null;setWaitingForRun(false);setError(e instanceof ApiError?e.message:"The release could not be started.");setCompletion("failure")}finally{setTriggering(false)}}async function stopRelease(){if(!trackedRun||isFinished(trackedRun))return;setStopping(true);setError(null);try{const result=await api.cancelAdminRelease(trackedRun.id);setMessage(result.message);window.setTimeout(()=>void refresh(),800)}catch(e){setStopping(false);setError(e instanceof ApiError?e.message:"The release could not be stopped.");setCompletion("failure")}}const ready=Boolean(status?.releaseConfigured),run=trackedRun??status?.latestRun??null,releaseActive=Boolean(releaseStart.current)&&(!trackedRun||!isFinished(trackedRun)),format=(v?:string|null)=>v?new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(new Date(v)):"Not available";const stages=[{label:"Request",hint:"Send release to GitHub",stepNames:[] as string[]},{label:"Checks",hint:"Build and quality checks",stepNames:["run quality checks"]},{label:"Database",hint:"Apply D1 migrations",stepNames:["apply pending d1 migrations"]},{label:"Deploy",hint:"Publish the Worker",stepNames:["deploy production worker"]},{label:"Verify",hint:"Confirm live health",stepNames:["verify production health","mark verified production release"]}],steps=trackedRun?.steps??[];function rawStageState(index:number):StageState{if(!releaseStart.current&&!trackedRun)return"idle";if(index===0)return trackedRun?"done":"active";const stage=stages[index];if(!stage)return"idle";const matching=steps.filter(step=>stage.stepNames.some(name=>step.name.toLowerCase().includes(name)));if(matching.some(step=>step.conclusion&&step.conclusion!=="success"&&step.conclusion!=="skipped"))return"failed";if(matching.some(step=>step.status==="in_progress"))return"active";if(matching.length&&matching.every(step=>step.status==="completed"&&(step.conclusion==="success"||step.conclusion==="skipped")))return"done";if(trackedRun&&trackedRun.status!=="completed"&&index===1&&steps.length===0)return"active";return"idle"}const stageStates:StageState[]=stages.map((_,index)=>rawStageState(index));for(let index=1;index<stageStates.length;index++){const previous=stageStates[index-1];if(previous!=="done"&&stageStates[index]!=="failed")stageStates[index]="idle"}if(trackedRun?.status==="completed"&&trackedRun.conclusion==="success")for(let index=0;index<stageStates.length;index++)stageStates[index]="done";
return <main className={`admin-page admin-theme-${theme}`}><header className="admin-header"><button className="admin-back" onClick={onBack}><ArrowLeft/> Back to Kit Hub</button><div className="admin-title"><span><ShieldCheck/></span><div><small>KIT HUB ADMIN</small><h1>Kit Hub control room</h1><p>Production releases and tester feedback in one operations space.</p></div></div></header><section className="admin-toolbar"><div><Palette/><span><strong>Admin colour</strong><small>Separate this space from the family hub.</small></span></div><div className="admin-themes">{themes.map(item=><button key={item.key} type="button" className={theme===item.key?"selected":""} onClick={()=>setTheme(item.key)}><i style={{background:item.hex}}/>{item.label}{theme===item.key&&<Check/>}</button>)}</div></section><section className="admin-hero"><div><b>PRODUCTION</b><h2>Release <code>main</code> with every step visible.</h2><p>Request, checks, database migration, Worker deployment and live verification stay together.</p></div><div className={ready?"admin-health ready":"admin-health setup"}>{ready?<CheckCircle2/>:<TriangleAlert/>}<span><strong>{ready?"Release bridge ready":"One-time setup required"}</strong><small>{ready?"Server-side GitHub trigger configured":"Add the GitHub release token secret"}</small></span></div></section>{releaseActive&&<section className="admin-progress"><header><div><LoaderCircle className="spin"/><span><strong>{stopping?"Stopping production release…":trackedRun?"Production release is running":"Waiting for GitHub to start the run"}</strong><small>This page updates automatically.</small></span></div><div className="admin-progress-actions"><b>{trackedRun?"LIVE":"STARTING"}</b>{trackedRun&&!isFinished(trackedRun)&&<button className="admin-stop" onClick={()=>void stopRelease()} disabled={stopping}><Square/>{stopping?"Stopping…":"Stop release"}</button>}</div></header><div className="admin-stage-list">{stages.map((stage,index)=>{const state=stageStates[index]??"idle";return <div className={`admin-stage ${state}`} key={stage.label}><span>{state==="done"?<Check/>:state==="failed"?<X/>:state==="active"?<LoaderCircle className="spin"/>:index+1}</span><div><strong>{stage.label}</strong><small>{stage.hint}</small></div></div>})}</div></section>}{error&&<div className="admin-alert error"><TriangleAlert/><span><strong>Couldn&apos;t complete that action</strong><small>{error}</small></span></div>}{message&&releaseActive&&<div className="admin-alert success"><CheckCircle2/><span><strong>{stopping?"Cancellation requested":"Release requested"}</strong><small>{message}</small></span></div>}<div className="admin-grid"><section className="admin-card"><header><Rocket/><div><small>PRIMARY ACTION</small><h2>Sync + release + verify</h2></div></header><p>Runs the protected production workflow and follows it until the final result.</p><button className="admin-primary" onClick={()=>void triggerRelease()} disabled={!ready||triggering||releaseActive}>{triggering||releaseActive?<LoaderCircle className="spin"/>:<Rocket/>}{triggering?"Starting release…":releaseActive?"Release in progress…":"Sync + release + verify"}</button></section><section className="admin-card"><header><CloudCog/><div><small>LIVE WORKER</small><h2>Current deployment</h2></div></header>{loading?<p>Loading deployment metadata…</p>:<dl><div><dt>Version</dt><dd>{status?.deployedVersion.id??"Unknown"}</dd></div><div><dt>Tag</dt><dd>{status?.deployedVersion.tag??"No tag"}</dd></div><div><dt>Deployed</dt><dd>{format(status?.deployedVersion.timestamp)}</dd></div></dl>}</section><section className="admin-card wide"><header><GitBranch/><div><small>GITHUB ACTIONS</small><h2>Latest production release</h2></div><button className="admin-secondary" onClick={()=>void refresh()}><RefreshCw/>Refresh</button></header>{run?<div className="admin-run"><strong>{run.status!=="completed"?"Release in progress":run.conclusion==="success"?"Release succeeded":run.conclusion==="cancelled"?"Release cancelled":"Release needs attention"}</strong><small>{run.name} · {run.headBranch} · {format(run.updatedAt)}</small>{run.failure&&<div className="admin-failure-summary"><b>Failed: {run.failure.step}</b><span>{run.failure.summary}</span>{run.failure.excerpt&&<pre>{run.failure.excerpt}</pre>}</div>}{run.htmlUrl&&<a href={run.htmlUrl} target="_blank" rel="noreferrer">Open run <ExternalLink/></a>}</div>:<p>No production release workflow run is available yet.</p>}</section></div><AdminFeedbackBoard/><section className="admin-safety"><ShieldCheck/><div><strong>Admin safety boundary</strong><p>Only server-side allowlisted platform administrators can use release and tester-feedback administration.</p></div></section>{completion&&<div className="admin-modal-backdrop"><section className={`admin-completion ${completion}`} role="dialog" aria-modal="true"><button className="admin-modal-close" onClick={()=>setCompletion(null)}><X/></button>{completion==="success"?<CheckCircle2/>:<TriangleAlert/>}<small>PRODUCTION RELEASE</small><h2>{completion==="success"?"Kit Hub is live and verified":completion==="cancelled"?"Release stopped":"Release needs attention"}</h2>{completion==="failure"&&run?.failure?<div className="admin-failure-detail"><strong>{run.failure.step}</strong><p>{run.failure.summary}</p>{run.failure.excerpt&&<pre>{run.failure.excerpt}</pre>}</div>:<p>{completion==="success"?"Checks passed, database work completed, the Worker deployed, and production verification finished successfully.":completion==="cancelled"?"The active production workflow was cancelled.":error??"A release step failed."}</p>}<div className="admin-modal-actions"><button className="admin-primary" onClick={()=>setCompletion(null)}>Done</button>{run?.htmlUrl&&<a className="admin-secondary" href={run.htmlUrl} target="_blank" rel="noreferrer">Open GitHub run <ExternalLink/></a>}</div></section></div>}</main>}
